# Paco Overview

Reusable read-only observations about Paco product areas. This file records current UI behavior, not product intent or ticket requirements.

## Survey status

- Coverage: `Partial`
- Last environment: `dev` (`https://blinx.dev.blinxpaco-np.com`)
- Last role: `Super Admin GB`
- Last surveyed: `2026-09-16`
- Views surveyed: 81 cumulative (48 through batch 25 + 7 in batch 26 + 9 in batch 27 + 4 in batch 28 + 7 in batch 29 + 6 in batch 30).
- `[Observed: dev, Super Admin GB, 2026-09-16]` Batch 29 resolved automation access to `Patient Search > Actions > Profile`: AG Grid exposes the stable cell attribute `[col-id="actions"]`, and its row button opens the menu containing the read-only `Profile` entry. Seven meaningful states were captured without mutation: Patient Profile `Dashboard`, `Timeline`, `Coding`, `Documents`, `Investigations > Results`, `Investigations > Requested`, and `Payments`. Reusable detail: [`workflows/patient-profile.md`](workflows/patient-profile.md).
- `[Observed: dev, Super Admin GB, 2026-09-16]` Batch 30 re-opened remaining Configuration gaps through visible navigation. `Appointment Books > Scheduler` reached `/configuration/#scheduler-config`, rendered heading `SCHEDULER CONFIG`, then remained at `Loading...` after bounded wait. `Clinical Config > Template Library > Prescribing Formularies` did not navigate away from `/paco/configuration/` and retained `Please select a menu item`. Direct re-check of `/configuration/#care-navigation-config` rendered `CARE NAVIGATION CONFIG` plus header context but no configuration body. Reusable detail updated: [`workflows/configuration.md`](workflows/configuration.md).
- `[Observed: dev, Super Admin GB, 2026-09-16]` Batch 31 re-checked all three `Web Chat & Video` routes. After bounded wait 8 giây, `Media Library`, `Virtual Appointments`, và `Outstanding Reviews` chỉ render Paco shell/header; không render feature body từng thấy với role `GP - paco assist`. `Virtual Appointments` emitted HTTP `500`/`401` console errors plus a page exception; `Outstanding Reviews` emitted `403`/`401`; `Media Library` emitted `PACOMMS` permission-setting and shared-module warnings. Đây là role/session-dependent observed regression, chưa có trusted basis để gán một warning cụ thể làm root cause. Reusable detail: [`workflows/web-chat-video.md`](workflows/web-chat-video.md).
- `[Observed: dev, Super Admin GB, 2026-09-16]` Batch 32 selected new area `Analytics & Reports > Capacity & Demand`. `/capacity-demand/` (app redirects from `/paco/capacity-demand/` to root-level path) rendered a populated `All Time` dashboard with `CLINICIAN CAPACITY`, `CONSULTATION OBSERVATIONS`, `APPT. DURATION: PLANNED vs ACTUAL`, `APPT. TIME BY SESSION`, `APPT. SEEN vs DNA`, và `DNA BY CLINICIAN`. Visible selections included six clinician-role categories and five consultation observations. Date-range control uses class `.date-range-picker__input-field--outline`; clicking it opens a `react-datepicker` dropdown with 20 preset options. The batch 32 "top-bar interception" was a timing artifact (SPA not mounted when click attempted); the control is fully clickable once `CLINICIAN CAPACITY` is present in DOM. Console shows HTTP `500` and "Error fetching user provider roles" without visible impact. A follow-up filter interaction test (2026-09-16) confirmed: date container click opens dropdown (JS evaluate needed in CDP mode); preset "This Month" opens calendar view (not a direct selection); Doctor chip click is stable without force; observation chips are icon-only (15 of 22 total chips have no visible text). Reusable detail: [`workflows/capacity-demand.md`](workflows/capacity-demand.md).
- Resume checkpoint: `Capacity & Demand` — full filter system mapped and confirmed. 3 chip rows: role (Y≈410), observation (Y≈186), session/appointment type (Y≈560). All removable via `dispatchEvent(MouseEvent)` on `[class*="deleteIcon"]`. 6 Highcharts charts, 5 card info buttons (tooltip on click), "More options" menu (PaComms/Quick View/PACO Assist), "Sessions Selected" clickable filter, "Clinicians Selected" display-only. Date range: preset → calendar → Apply. Open: observation combobox search-to-add, chart tooltip click, session chip metric impact, "Clinicians Selected" expand. Next: survey new workflow (patient-profile or web-chat-video).
- `[Observed: dev, GP - paco assist, 2026-09-14]` A prior run ended early after automation triggered an unintended `Sign Out` while on the broken `Patients > Care Navigation` page. The tester re-authenticated manually before this run; this run added stricter selector guards (exact text match, position disambiguation, unsafe-URL circuit breaker) and completed without further mutation.
- `[Observed: dev, GP - paco assist, 2026-09-14]` Between batches, the manual login browser (`npm run auth:login`) was closed by the tester and had to be relaunched and re-authenticated before the survey could resume; the skill correctly reported `Blocked: Authentication expired` rather than attempting to proceed or requesting credentials.
- `[Observed: dev, GP - paco assist, 2026-09-14]` Separately, the browser session was also once signed out automatically ("We couldn't confirm your session was still active, so you were signed out for security") mid-survey, unrelated to any automation action; the tester re-authenticated manually before the run resumed.

## Product areas

### Dashboard

- `[Observed: dev, GP - paco assist, 2026-09-14]` Page title is `PACO OS`; entry path is `/paco/dashboard`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` Dashboard exposes time ranges `day`, `week`, `month`, `year` and pagination controls `Page 1` through `Page 4`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` Dashboard includes `Opel level`, call-status charts and `Staff Availability`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` Sidebar supports collapsed and expanded states.

### Sidebar map

- `[Observed: dev, GP - paco assist, 2026-09-14]` `Dashboards`: `Dashboard`, `Manager`, `Connect`, `Clinician`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Analytics & Reports`: `Capacity & Demand`, `Patient Analyser`, `Medication Analyser`, `QOF Registers`, `Patient Details`, `Comms Analytics`, `Reports`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Comms Hub`: `Template Manager`, `Campaign Manager`, `Patient Manager`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Health Forms`: `Inbox`, `Designer`, `Designer V2`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Patients`: `Patient Search`, `Care Navigation`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Web Chat & Video`: `Virtual Appointments`, `Media Library`, `Outstanding Reviews`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Case Load Management`: `My Case`, `Unallocated`, `Case Workboards`, `Task Workboards`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Appointment Book`: `Appointment Book`, `Appointment Settings`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Quick Pay` opens `/paco-connect/quick-pay/invoices` with page title `PACO Connect`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `User Portal` opens `/paco-connect/user-training-portal` with page title `PACO Connect`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Configuration` opens `/paco/configuration/organisation/general`; visible sections include `Basic Information`, `Location & Contact Information`, `Opening Hours`, and `Communications`. Mutation controls were not used.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Rocket Bar` exposes `Launch Rocket Bar` and `Download Rocket Bar`; neither action was used.
- `[Observed: dev, GP - paco assist, 2026-09-14]` Sidebar can render as a collapsed icon rail; clicking a group icon in this state opens a floating flyout listing that group's child items (e.g. `Patients` flyout shows `Patient Search` and `Care Navigation`) instead of navigating directly.
- `[Observed: dev, GP - paco assist, 2026-09-14]` A `Dev Menu Item` entry is present near the bottom of the sidebar, below `Rocket Bar`; its purpose was not explored.
- `[Observed: dev, GP - paco assist, 2026-09-14]` For some groups (`Web Chat & Video`, `Appointment Book`), clicking the group icon on a fresh dashboard load navigates directly to that group's first/default child route (a distinct themed sub-app) rather than opening a flyout; the same shared icon rail is present inside that sub-app, and clicking the (now active) group icon again there opens a flyout listing its sibling child items.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Web Chat & Video` and `Appointment Book` sub-apps use a visually distinct dark theme from the main `PACO OS` sidebar, but share the same icon rail component.
- `[Observed: dev, GP - paco assist, 2026-09-14]` When the icon rail is in its collapsed (narrow) state, icons for `Comms Hub`, `Health Forms`, and `Quick Pay` are not shown at all (not just visually hidden) even though `Dashboards`, `Analytics & Reports`, `Patients`, `Web Chat & Video`, `Case Load Management`, `Appointment Book`, `User Portal`, `Configuration`, and `Rocket Bar` icons remain visible; this was observed consistently across the main dashboard and both sub-apps in this run. This narrowed set may explain the earlier `[Open Question]` about those items appearing missing.

### Child routes — batch 1

- `[Observed: dev, GP - paco assist, 2026-09-14]` `Dashboards > Dashboard` opens `/paco/dashboard`; visible functions include patient search, time-range charts, pagination and staff availability.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Dashboards > Manager` opens `/paco/dashboard/manager-dashboards/overview`; visible areas include organisation tasks, staff performance/stats, patients, CQC readiness, message outbox and comms analytics.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Dashboards > Connect` opens `/paco-connect/dashboard` with title `PACO Connect`; no stable visible landmarks were captured.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Dashboards > Clinician` opens `/paco/dashboard/clinician-dashboards/main`; visible areas include tasks, appointments, prescriptions, results, documents, health-form inbox and clinician stats.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Analytics & Reports > Capacity & Demand` opens `/capacity-demand/`; page content did not expose stable landmarks in this run.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Analytics & Reports > Patient Analyser` opens `/patient-analyser-new/`; page content did not expose stable landmarks in this run.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Medication Analyser`, `QOF Registers` and `Patient Details` each landed on `/patient-analyser-new/`; exact destination behavior needs confirmation.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Analytics & Reports > Comms Analytics` landed on `/commshub/login?loggedout=true&msg=error-at-axios-interceptor`, showing `Welcome Back` and `Sign In`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Analytics & Reports > Reports` opens `/paco/analytics-reports/scr-reports`; visible functions include consent search, export, columns and filters over SCR records.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Comms Hub > Template Manager` landed on the same Comms Hub login/error route instead of feature content. Note: in batch 4 (same day, later run), `Template Manager` and `Campaign Manager` both loaded successfully on the real Comms Hub app, while `Patient Manager` still hit this same login/error route — so the redirect appears to be either session-timing-dependent or specific to the `Patient Manager` route rather than the whole Comms Hub app being down.

### Child routes — batch 2

- `[Observed: dev, GP - paco assist, 2026-09-14]` `Patients > Patient Search` opens `/paco/patient-search`; visible functions include a patient search field, `Filters`, `Show archived`, `Create new patient`, and a results table with columns `Registration Type`, `Title`, `First Name`, `Middle Name`, `Last Name`, `Actions`. The table showed an empty/loading state (`Patient Search (0)`) at capture time.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Patients > Care Navigation` navigated to `/patient-search/` (no `/paco` prefix) and rendered a blank page titled `Application error: a client-side exception has occurred`.

### Child routes — batch 3

- `[Observed: dev, GP - paco assist, 2026-09-14]` `Web Chat & Video > Virtual Appointments` opens `/web-chat/appointments/` (breadcrumb `Web Chat & Video > Appointments`, dark theme); visible functions include an `Appt. Type` filter panel (`All Appointment Types`, `Pre-booked Appointments` with `Web Chats`/`Video Consultations` counts, `On Demand Appointments`), a `Date Range` selector, and a results table (`Patient Name`, `Appt. Type`, `Status`, `Actions`) with `Columns`/`Filters` side controls.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Web Chat & Video > Media Library` opens `/web-chat/media-library`; visible functions include a `Patient` search field and a `Media Type` filter (`Video` seen); shows a `No Patient Selected` placeholder until a patient is chosen.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Web Chat & Video > Outstanding Reviews` opens `/web-chat/review` (breadcrumb `Web Chat & Video > Consultations`, heading `CONSULTATIONS FOR REVIEW`); table columns include `Patient Name`, `Appt. Time`, `Pt. Arrived`, `Appt. Type`, `Action`, with `Columns`/`Filters` side controls. Breadcrumb label (`Consultations`) differs from the sidebar label (`Outstanding Reviews`).
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Case Load Management > My Case` opens `/paco/my-case`; showed a `Getting your case` loading state at capture time.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Case Load Management > Unallocated` opens `/paco/unallocated`; visible functions include `Search this board`, `List View`, `Create new case`, a `Breaching (high to low)` sort control, `Filters`, an `All` tab, and card columns (loading state at capture time).
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Case Load Management > Case Workboards` and `Task Workboards` both open `/paco/workboards` (`?tab=case` and `?tab=task`); shared `Work Boards` panel includes `View My Tasks Only`, `View My Cases Only`, a `Search all boards` field with `Board`/`Case ID`/`Patient` modes, `Tasks (0)`/`Cases (0)` counts, and `Create new board`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Appointment Book > Appointment Book` opens `/paco-connect/appointment-book` (title `PACO Connect`); visible functions include `Day`/`Week`/`Month`/`Year` views, a date picker with `Go to today`, `Sessions`/`Booked`/`Available`/`LIVE` counters, `Select Book`, and a filter control.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Appointment Book > Appointment Settings` opens `/paco-connect/configuration` (title `PACO Connect`, heading `Configuration`); visible sections include `Organisation`, `Patient`, `Appointment Books`, `Quick Pay`, `Patients & Proxy`, `Users & Staff`, `Clinical Config`, and `Case Prioritisation`. This is a distinct `Configuration` surface from the one opened by the main sidebar's `Configuration` item (`/paco/configuration/organisation/general`).

### Child routes — batch 4

- `[Observed: dev, GP - paco assist, 2026-09-14]` `Analytics & Reports > Medication Analyser` opens `/patient-analyser-new/` (breadcrumb `Analytics > Patient & Medication Analyser`); the page is a single app with an in-page tab strip (`PATIENT ANALYSER`, `MEDICATION ANALYSER`, `QOF REGISTERS`, `PATIENT DETAILS`) plus a `Reports` side panel (`Blinx Report`, `Practice Report`), search, `Advanced Search`, `Save Report`, `Reset View`/`Expand All`/`Collapse All`, and a results table with columns including `Age (Current)`, `Days Left`, `Drug Record Prescription`. This explains the earlier open question: `Medication Analyser`, `QOF Registers`, and `Patient Details` are tabs of one app at the same route, not separate broken links.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Analytics & Reports > QOF Registers` and `Analytics & Reports > Patient Details` both navigated to the same `/patient-analyser-new/` route but rendered a blank white page at capture time (~1.5s after navigation); unlike the `Medication Analyser` capture (taken with more render time), no tab content had painted yet. Re-checked in batch 5 with a ~5-8s wait: the app renders fully (tab strip `PATIENT ANALYSER`/`MEDICATION ANALYSER`/`QOF REGISTERS`/`PATIENT DETAILS`, `Reports` panel, search, results table) — the blank captures were a timing artifact of a slow-loading app, not a real empty state. Tab-switch behavior when clicking `QOF Registers`/`Patient Details` from the sidebar (vs. clicking the in-page tab) is still unconfirmed.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Comms Hub > Template Manager` opens `https://nhs-comms-hub-dev.blinxhealthcare.com/commshub/template-builder` (external domain, dark theme, title `Dashboard`); visible functions include `Show Org. Default Templates`, `Create New Email`, `Create New SMS`, `Search Templates`, and column/filter controls over a templates table (empty at capture time).
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Comms Hub > Campaign Manager` opens `https://nhs-comms-hub-dev.blinxhealthcare.com/commshub/campaign-manager`; visible functions include `Create Campaign`, `Campaign Outbox`, a `Filter Campaigns` panel with `Campaign Statuses` (`Draft`, `Inactive`, `Queued`, `In Progress`, `Sent`, `Failed`, `Paused`, `Deleted`, `Available Quick Send`, ...), search, and column/filter controls over a campaigns table.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Comms Hub > Patient Manager` consistently navigates to `https://nhs-comms-hub-dev.blinxhealthcare.com/commshub/login?loggedout=true&msg=error-at-axios-interceptor` (`Welcome Back` / `Sign In` / `Username and Password` screen), confirmed on a retry in the same run, while `Template Manager` and `Campaign Manager` in the same Comms Hub app load correctly. This looks specific to the `Patient Manager` route rather than a whole-app outage.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Health Forms > Inbox` opens `/health-forms/responses/` (breadcrumb `Health Forms > Health Form Inbox`, title `Health Forms`); page rendered blank white at capture time (~1.5s after navigation) — re-checked in batch 5 with a ~5-8s wait and rendered fully: stat tiles `Total Sent to Patient`, `Incomplete`, `Overdue from Patient`, `Completed by Patient`, `Awaiting Review`, `Reviewed`, plus two tables `Completed By Patient - Alarm Triggered` and `Completed By Patient - No Alarm Triggered` with `Health Form`/`Synced`/`Action` columns and a `Review Form` action. The earlier blank capture was a timing artifact.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Health Forms > Designer` opens `/health-forms/builder/` (breadcrumb `Health Forms > Health Form Designer`, title `Health Forms`); page rendered as a solid gray placeholder at capture time — re-checked in batch 5 and rendered fully: `New Health Form` action and a table with columns `Health Form type`, `Health Form Reviewer`, `Health Form Name`, `Created Date`, `Created By`, `Created By Organisation`, `Updated By`, `Shared To Organisation(s)`. The earlier gray capture was a timing artifact.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Health Forms > Designer V2` opens `/paco/health-forms` (title `PACO OS`, same domain as the main app, unlike `Inbox`/`Designer` which are on a separate `/health-forms/` path); page rendered blank at capture time — re-checked in batch 5 and rendered fully: heading `Health Form Templates`, `Filters`, a `+` create action, and a results table with columns `Name`, `Type`, `Created date`, `Created by`, `Updated date`, `Actions`. The earlier blank capture was a timing artifact; `Designer` and `Designer V2` remain visually and structurally distinct apps (different domain path, different table columns), so they are likely two different form-builder implementations rather than simple versions of the same tool.

### Patient Search — in-page content (batch 6)

- `[Observed: dev, GP - paco assist, 2026-09-14]` The header's global search field (`Search patients by name or NHS number`) is a separate control from the in-page `Search Patients...` field on `/paco/patient-search`. Typing in the header field opens a floating quick-search popover with its own `Results (N)` count and an `Include deleted patients` checkbox, independent of the page's own list.
- `[Observed: dev, GP - paco assist, 2026-09-14]` The in-page `Search Patients...` field filters the results grid in place (e.g. typing `Abbott` narrowed `Patient Search (78,779)` down to `Patient Search (2)`); the results grid is an AG Grid instance (`role="grid"`/`role="row"`/`role="gridcell"`, `.ag-*` classes) with many more data columns available than the default visible set (`registration_type`, `title`, `givenname`, `middlename`, `surname`, `known_as`, `birth_datetime`, `death_datetime`, `gender`, `nhs_number`, `org_patient_identifier`, `marital_status`, `ethnicity`, `birth_country`, `entry_date_uk`, `country_language.language_name`, `interpreter_required_ind`, `religion`, `home_number`, `mobile_number`, `email_address`, `preferred_communication_method` (×2), `sms_option_ind`, `email_option_ind`, address fields, `usual_gp_name`, `hospital_number`, `registration_date`), presumably togglable via the `Columns` control seen elsewhere in the app.
- `[Observed: dev, GP - paco assist, 2026-09-14]` Each row's `Actions` column opens a menu with `Profile`, `View`, `Edit`, `View Audit`, `Archive`, `Save for offline access`. Only `View` was opened in this run (read-only); `Edit`/`Archive`/`Profile`/`Save for offline access` were seen but not clicked.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Actions > View` opens a `View Patient` modal dialog over the page (not a route change) with tabs `Patient`, `Personal`, `Contact`, `Address` (warning icon), `Healthcare Provider` (warning icon), `Relationships & Permissions`. `Patient` tab shows `Patient Type`/`Registration Type` and a `Last Name`/`Date of Birth`/`Postcode` search block with a `PDS Check` link. `Personal` shows `Name & Identity` fields (Title, First Name(s), Middle Name, Last Name, Suffix, Known As, Previous Family Name, Marital Status, Ethnicity, Country of Birth, Date of Entry to UK, NHS Number). `Contact` shows `Phone Numbers` (with an `Add Another` link, not clicked) and `Additional Contact Information` (Primary Email, Preferred Contact Method, Confirmed By). `Relationships & Permissions` shows the patient as `Primary Patient` and a `Household Members` panel (empty: "No household members added yet — Household members are relationships whose address matches the patient's address"). The `Address` and `Healthcare Provider` tabs carry a warning icon in the tab strip, suggesting missing/incomplete data for this demo patient, but their content was not opened.
- `[Observed: dev, GP - paco assist, 2026-09-14]` The page-level `Filters` button opens a right-side drawer (not the same as the `Actions` menu) with dropdown filters: `Title`, `Age`, `Gender`, `Ethnicity`, `Country of Birth`, `Language` (list continued below the fold), plus `Cancel`/`Save` and a `Clear all` link. Closed via `Cancel` without changing any filter.
- `[Observed: dev, GP - paco assist, 2026-09-14]` **(batch 12)** Opened the previously-unexplored `Address` and `Healthcare Provider` tabs on the same demo patient's `View Patient` dialog: `Address` shows a `Primary Address` block (`Address Type`, `Address Line 1` *required*, `Address Line 2`, `Town`, `County`, `City`, `Postcode` *required*, `Access Instructions For Address`) with an inline validation message `Address Line 1 is required` displayed even in view mode. `Healthcare Provider` shows `Usual GP` *required* (inline `Usual GP is required` message), `Practice` (also flagged with its own small warning icon), `Hospital Number` (with an `Add Another` link, not clicked), and further fields below the fold (`Records At`, `Old NHS Number`). This confirms the tab-strip warning icons mean "this tab has unmet required-field validation for this patient," not a system error.
- `[Observed: dev, GP - paco assist, 2026-09-14]` **(batch 13)** `Actions > Profile` (distinct from `Actions > View`) does NOT open a modal — it navigates to a full route, `/paco/patient-profile/<patient-guid>/dashboard`, i.e. a complete clinical patient-record module separate from the `View Patient` dialog. Its header shows patient name/NHS number/DOB/age plus a `+` action (not clicked, likely "add" something). Its tab strip (more tabs than fit on screen, scrollable via an arrow control) includes at least `Dashboard`, `Timeline`, `Coding`, `Consultations & Admin`, `Medication`, `Patient Comms`, and more. The `Dashboard` tab is a customizable widget grid (`Customise Dashboard`, `13 Selected`) with widgets seen: `Conditions` (Active/Severe/Moderate/Minor counts, `No active conditions` for this patient), a registers widget (`Patient isn't currently on any registers`), and a `Pending Documents` widget, plus `Trends` and `Health Report` buttons. This whole patient-profile module was previously unmapped in this survey and likely warrants its own dedicated exploration pass.
- `[Observed: dev, GP - paco assist, 2026-09-14]` **(batch 13) Defect**: on the patient-profile `Dashboard` tab, one widget shows a raw error message instead of content or a graceful empty state: **"Error loading pharmacies"**, with no retry control and no further detail.

### Unallocated — in-page content (batch 7)

- `[Observed: dev, GP - paco assist, 2026-09-14]` `/paco/unallocated` shows a kanban-style board of case cards grouped by an OOH (out-of-hours) breach-time label (e.g. `2 Hours OOH`, `6 Hours OOH`, `20 Mins OOH`) colored by urgency (blue for longer time remaining, pink/magenta for imminent breach). Each card shows patient name, age, gender, phone, `Active Time`, address, and a `Pathway` link, plus `Request info` and `Add to Board` actions (not clicked).
- `[Observed: dev, GP - paco assist, 2026-09-14]` A priority tab strip above the board (`All (617)`, `Mid Priority (508)`, `High Priority (17)`, `Low Priority (23)`, `white (1)`, `Emergency (68)`) filters which cards display; the page's own `Unallocated (617)` heading count does not change when a tab is selected, only the card list below does. The `white (1)` tab label appears to be an unlocalized/placeholder priority name (lowercase, generic color word) rather than a proper priority label like the others.
- `[Observed: dev, GP - paco assist, 2026-09-14]` Each card's `...` header icon (not a semantic `<button>`, just an SVG with a click handler) opens a context menu: `Change Priority`, `View PDS`, `View Audit`, `Assign Skills`, `Quick Book`, `Quick Send`, `Close Case`, `Flag`, `Move Case`, `Manual Call`. Only `View Audit` was opened in this run; `View PDS` was intentionally not clicked since it may call an external NHS PDS lookup rather than a local read; all other items are mutation-shaped and were not clicked.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `View Audit` opens an `Audit Trail` modal that, for role `GP - paco assist`, fails with: "Could not load audit data. You don't have permission to view Audits logs. Ask an administrator to enable this for your role." (with a `Retry` control). This directly confirms `GP - paco assist` lacks audit-log viewing permission — a partial answer to the open question about what this role can/cannot do.

### Health Form Inbox — in-page content (batch 8)

- `[Observed: dev, GP - paco assist, 2026-09-14]` Above the two "Completed By Patient" tables (`Alarm Triggered` / `No Alarm Triggered`), each row's `Action` column has a `Review Form` button plus a separate blue paper-plane-style icon button next to it (distinct pinned-right grid cell); neither was clicked since `Review Form` is a review/mark-reviewed mutation and the paper-plane icon's function was not confirmed. No plain "view" affordance was found for these two tables — clicking the health form name text itself does not open anything.
- `[Observed: dev, GP - paco assist, 2026-09-14]` Below the two tables, a larger `All Health Forms` panel provides its own left-hand status tree (`All (58,386)`, `Incomplete (47,273)`, `Overdue (11,642)`, `Completed (11,113)`, `Awaiting Review (121)`, `Reviewed (10,992)` with sub-items `Synced to PR (4,809)` / `Not Synced to PR (6,183)`), a search box, `Expand All`/`Collapse All`, and a `View By Patient` / `View By Health Form` toggle. The default grouped view lists rows by patient name with an expandable count (e.g. `Simon Bowers (2)`) and columns `Patient Name`, `Health Form Name`, `Patient ID`, `Action`, `Synced`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` **Data/display defect**: in the `Awaiting Review` group-by-patient view, one row's `Patient Name` column renders the raw, untranslated aggregation object instead of a name: `{"patient_derived_fullname":null,"patient_patientguid":null,"count":"87","agg--response_actual_date":"2026-09-01T09:28:17.064Z"}`, grouping 87 forms. This happens specifically when the underlying `patient_derived_fullname`/`patient_patientguid` fields are `null` (i.e. forms not linked to an identified patient) — the UI has no fallback label (e.g. "Unknown Patient") for this case and instead leaks the raw JSON string into the visible grid cell. Expanding this row triggers a `Fetching your report...` loading state (row-level lazy-load of the 87 underlying forms); the expanded content did not stay visible long enough in this run to inspect.
- `[Observed: dev, GP - paco assist, 2026-09-14]` **(batch 13)** Retried expanding the same JSON-leak row across several attempts: the page carries a visible `Refreshing in N seconds` countdown that periodically reloads the `All Health Forms` panel's data (the visible counts drift slightly between refreshes, e.g. `Awaiting Review (121)` → `(120)` → `(119)` as forms get reviewed elsewhere in the environment) and appears to reset any expanded-row state back to collapsed each cycle. This made it impractical to keep the group expanded long enough to inspect its 87 underlying forms in this run — the auto-refresh interval is shorter than the time needed to read an expanded group's content.

### Case Workboards — in-page content (batch 9)

- `[Observed: dev, GP - paco assist, 2026-09-14]` `/paco/workboards?tab=case` resolves to a specific board URL (e.g. `/paco/workboards/106`) rather than staying on the query-param route; the left panel lists all boards with per-board case counts (most were `(0)`, one demo board had `(0)` despite being the default-selected board, showing a `Case Board Empty` state). The board list and global `Tasks (N)`/`Cases (N)` counts take several seconds to populate after navigation — another instance of the slow-loading-app pattern seen in batches 4/5/8.
- `[Observed: dev, GP - paco assist, 2026-09-14]` One demo board is titled "Epsom PCN Demo - Care Home VIsts" — likely a typo (`VIsts` instead of `Visits`), worth a spelling fix if this is real seed/demo data rather than throwaway test content.
- `[Observed: dev, GP - paco assist, 2026-09-14]` A populated board (`waiting list`, 8 cases) shows the same kanban-card layout as `Unallocated`: urgency-colored header (e.g. `20 mins CAS`), `Received: <date time>`, patient name/age/gender, `Active Time`, a flag-with-count badge, and `Time to breach` (negative values observed, e.g. `-23d 21h 42m`, indicating already-breached cases still shown without a distinct "breached" visual state beyond the countdown going negative). Priority sub-tabs (`All`, `Low Priority`, `Emergency`) filter the board like on `Unallocated`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` Each card's `...` menu here has the same items as `Unallocated`'s (`Change Priority`, `View PDS`, `View Audit`, `Assign Skills`, `Quick Book`, `Quick Send`, `Close Case`, `Flag`, `Move Case`) plus two more: `Assign Case`, `Unassign assignee` — confirming this is a shared card-menu component across board types. `View Audit` was opened again here and failed with the identical permission error as on `Unallocated` ("You don't have permission to view Audits logs..."), confirming the restriction is role-wide, not board-specific.
- `[Observed: dev, GP - paco assist, 2026-09-14]` A `List View` / grid-view toggle switches the board between kanban cards and a table (columns `Date`, `Indicators`, `Case Status`, `Priority`, `Actions`, with a per-row `•••` action menu) plus `Group by:` options (`By Priority`, `By Pathway`, `By Incoming Service`, `By PDS Status`, `By Age Range`, `By Case Status`), none of which were applied in this run.
- `[Observed: dev, GP - paco assist, 2026-09-14]` A separate board-level `...` menu (top-right, distinct from the per-card menu) offers `Edit` and `Archive` for the whole board; neither was clicked.
- `[Observed: dev, GP - paco assist, 2026-09-14]` **(batch 13)** Applied `Group by: By Priority` on the `waiting list` board's List View: the table collapses into one row per priority group (e.g. `12 Hours OOH (1)`, `20 mins CAS (7)`), and a `Clear grouping` link appears. The column set also changes when grouped, adding `Case Score`, `Case ID` (a clickable link, e.g. `85`), and `Viewer` columns not shown in the ungrouped view. Expanding a group (clicking its row chevron, not the label text) reveals the individual cases with `Date`, `Indicators`, `Case Status` (`Open`/`Paused`), and a per-row `•••` actions menu; some rows loaded progressively (`Loading` placeholders) after the group expanded.

### Task Workboards — in-page content (batch 10)

- `[Observed: dev, GP - paco assist, 2026-09-14]` `/paco/workboards?tab=task` resolves to a specific board (e.g. `/paco/workboards/62`); the left board list showed task counts per board (e.g. `C&M Integrated Neighbourhood Team (10,611)`, `My Board 2 (43)`, many boards at `(0)`), again taking several seconds to populate after navigation.
- `[Observed: dev, GP - paco assist, 2026-09-14]` Unlike `Unallocated`/`Case Workboards` (single list of cards with priority sub-tabs), a Task Workboard is a true multi-column kanban: columns represent workflow stages (e.g. `NHS 111 (3)`, `Health Professional Referrals ... `, `Awaiting Triage (10,584)`, further stages seen on horizontal scroll: `Community Nurse Home Visit List (1)`, another `Community Nurse Home Visit ...`, `Dental Referral ...`). Each card shows a colored task-type header (e.g. `Task for te...`, `Urgent: ov...`, `Test Scribe`, `Free Text`, `Mastercall I...`) with an age indicator (e.g. `4w`, `2w`, `3w 4d`), patient avatar/name/NHS Number, task title, `Description`, and `Assigned to`.
- `[Observed: dev, GP - paco assist, 2026-09-14]` One card's description read "Auto-created because a task on the demo board went overdue.", revealing an automatic overdue-escalation rule that generates follow-up tasks.
- `[Observed: dev, GP - paco assist, 2026-09-14]` Each task card's `...` menu is a distinct, task-specific menu (different from the case-card menu): `View`, `Edit`, `Change Priority`, `Flag`, `Move Task`, `Change Status`, `Duplicate`, `Link Task`, `Archive Task` — notably no `View Audit`/`View PDS`/`Assign Skills`/`Quick Book`/`Quick Send`/`Close Case` here. Only `View` was opened (read-only).
- `[Observed: dev, GP - paco assist, 2026-09-14]` `View` opens a task detail modal (e.g. "Task for testing PAC2-7436" — referencing a ticket-ID-style task name) showing patient identity (name, NHS number, DOB, gender, age), editable-looking `Board`/`Column`/`Status` dropdowns (not changed), `Description`, `Assigned to`, `Reviewer`, `Author`, `Column`, `Status`, `Priority`, `Date Created`, `Due Date`, and a `Journey` section.
- `[Observed: dev, GP - paco assist, 2026-09-14]` The `Journey` section shows a per-stage duration tracker (current stage badge + time-in-stage, e.g. `NHS 111 · 1mo 4w · current`, `TOTAL 1mo 4w`) and a `Time Remaining` progress bar that, like the breach countdowns seen on `Unallocated`/`Case Workboards`, continues into negative values once overdue (observed: `1 day` vs. `-58 day`) without a distinct overdue visual state. Below it, a `Comments` text box with an `Add` button was left untouched (mutation).

### Media Library — in-page content (batch 11)

- `[Observed: dev, GP - paco assist, 2026-09-14]` `/web-chat/media-library` has two visually-similar but distinct search inputs both with generic placeholder text `Search...`: (1) the global header patient-search overlay (a hidden/invisible control on this dark-themed page — the light header bar itself is not rendered here, but the underlying PrimeReact search input and its results overlay panel still exist in the DOM and function when targeted), and (2) the page's own `PATIENT` panel search field (a Material-UI input) intended for selecting a patient to view media for. A generic selector matching "any input with placeholder containing Search" resolves to the hidden global one first, not the visible in-page one — worth noting for anyone writing automation or documentation against this page.
- `[Observed: dev, GP - paco assist, 2026-09-14]` **Functional gap**: typing a known valid patient surname (e.g. `Abbott`, already confirmed to exist via `Patient Search`) into the Media Library's own `PATIENT` panel search field and triggering search (via typing, `Enter`, or clicking the search icon button) produces no visible result — no patient list appears, no "no results" message, no loading indicator. The panel remains showing `No Patient Selected` throughout. It is unclear whether this field requires a different trigger (e.g. exact match, minimum character count, a separate results area not yet located) or is genuinely non-functional for surname search in this environment.
- `[Observed: dev, GP - paco assist, 2026-09-14]` The `MEDIA TYPE` filter list shows four options: `Audio`, `Chat Transcript`, `Message Log`, `Video` (earlier batch 3 only recorded seeing `Video`). None were clicked since no patient was selected to filter media for.

### Patient Profile — clinical record module (batch 14)

- `[Observed: dev, GP - paco assist, 2026-09-14]` The full tab set of `/paco/patient-profile/<id>/...` is (in order): `Dashboard`, `Timeline`, `Coding`, `Consultations & Admin`, `Medication`, `Patient Comms`, `Shared Records`, `Investigations`, `Appointments`, `Documents`, `Payments`, `Tasks` — 12 tabs total, navigated via left/right scroll-arrow controls since they don't all fit on screen at once. Each tab has its own URL segment (e.g. `/timeline`, `/coding`, `/consultations`, `/medications`, `/patient-comms`, `/shared-records`, `/investigations`, `/appointments`, `/documents`, `/payments`, `/tasks`).
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Timeline` shows a chronological, card-based history of clinical events (e.g. `Consultation` cards marked `Draft`/`Completed`, with clinician, org, and time), filterable by year (1987–2026 for this patient) and by `Sector`/`Event Type`/`Organisation`/`Status` (all `0 Selected` by default, i.e. unfiltered/all).
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Coding` shows category counts (`All (0)`, `Conditions (0)`, `Observations (0)`, `Measurements (0)`, `Procedures (0)`) plus a `Risk Stratification` link and `Filters`; empty for this demo patient (`No coding found`).
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Consultations & Admin` is a real, populated record for this patient (`3 of 9` shown): a searchable, filterable list (`All`/`Draft`/`Completed`, `Consultations`/`Admin`, `Expand all`) of consultation entries with clinician, a `[SYSTEM] PACO GP #<id>` tag, org, date/time, type (e.g. `Face to Face`), and a `Condition` tag. `New Consultation`, `From template`, and `Add entry` (all mutations) sit above the list; each row's `•••` menu offers `Edit` and `View Audit`. Expanding a row (its own `>` chevron) reveals per-topic detail (`Topic N`, `Condition`, `History`) and `Consultation Start`/`Consultation End` timestamps.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Consultations & Admin`'s row `View Audit` fails with the exact same permission error seen on `Unallocated`/`Case Workboards` ("Consultation audit" dialog: "You don't have permission to view Audits logs. Ask an administrator to enable this for your role.") — a third independent confirmation that this is a role-wide restriction covering multiple, unrelated feature areas (case boards, task boards, and now clinical consultations), not a per-feature quirk.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Medication` shows `Synced`/`All`/`Acute`/`Repeat` and `All`/`Draft`/`Active`/`Past`/`Cancelled` filters, sort options (`Default`, `Compressed`), `More Filters`, and a `New Prescription` action (mutation); empty for this patient (`No records`).
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Patient Comms` shows only a `Date Selection` (`All time`) and a `View` control in this run — content did not render further detail at capture time.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Shared Records` requires a separate national-system login: it shows `GP Connect` / `Launch NCRS` links behind a `Smartcard Login Required` modal ("Please login with your NHS Smartcard to continue" / `Log in with my Care Identity`, NHS-branded). This is a hard boundary — the standard `GP - paco assist` app login does not grant access to this tab's content; it requires a real NHS Smartcard/Care Identity credential that a test account cannot substitute for.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Investigations` shows a `Results` / `Requested` toggle, a `+ New Request` action (mutation), and an `ICE` label (likely referencing the ICE pathology/investigations system integration).
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Appointments` shows an empty table with columns `Appt Type`, `Appointment Name`, `Date`, `Start Time`, `End Time`, `Status`, `Actions` — no appointments for this patient.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Documents` shows `Create Document`/`From Template`/`New Fit Note`/`Upload` actions (mutations), `All`/`Pending`/`Reviewed` and `All`/`Mine` filters, sort control, and an empty state (`Showing 0/0 documents`, "There are no patient attachments").
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Payments` rendered only a bare `Payments` heading with no further content in this run — inconclusive whether this is a genuine empty state, a permission gate, or a slow-loading widget (per the pattern seen elsewhere in the app).
- `[Observed: dev, Super Admin GB, 2026-09-16]` Batch 29 re-verified `Timeline`, `Coding`, `Documents`, `Investigations`, and `Payments` on a searched demo patient. `Timeline` showed `No timeline found`; `Coding` showed all category counts at `0` and `No coding found`; `Documents` showed `Showing 0/0 documents` and `There are no patient attachments`; `Investigations > Results` showed `No records`, while switching to `Requested` was a distinct safe state with `No records`; `Payments` again showed only its heading. These are observations for this patient, not trusted expected behavior for populated records.
- `[Observed: dev, Super Admin GB, 2026-09-16]` Batch 29 automation note: Patient Search is AG Grid, not an HTML table. A stable route to the row menu is `[col-id="actions"] button`, followed by the exact menu item `Profile`; generic row/button selectors did not expose the control reliably.
- `[Observed: dev, GP - paco assist, 2026-09-14]` `Tasks` is a patient-scoped view of the same task system seen in the standalone `Task Workboards`: `Active`/`Closed`/`All` filters, `Create New task` (mutation), sort (`Time on Board`), `Filters`, and the identical `Group by:` options (`By Task Type`, `By Patient`, `By Team/Stage`, `By Priority`, `By Author`) plus a `Time in Stage` column — confirming the task system is shared/cross-cutting rather than board-specific, and is also surfaced per-patient here. Empty for this patient in this run.

### Super Admin GB route comparison — batch 15

- `[Observed: dev, Super Admin GB, 2026-09-15]` Từ `/paco/dashboard`, sidebar mở rộng hiển thị cùng 8 nhóm và 4 route độc lập đã ghi nhận cho role `GP - paco assist`: `Dashboards`, `Analytics & Reports`, `Comms Hub`, `Health Forms`, `Patients`, `Web Chat & Video`, `Case Load Management`, `Appointment Book`; cùng `Quick Pay`, `User Portal`, `Configuration`, `Rocket Bar`.
- `[Observed: dev, Super Admin GB, 2026-09-15]` Child item hiển thị trong từng nhóm cũng khớp map cũ: `Dashboard`/`Manager`/`Connect`/`Clinician`; 7 mục Analytics; 3 mục Comms Hub; 3 mục Health Forms; 2 mục Patients; 3 mục Web Chat & Video; 4 mục Case Load Management; 2 mục Appointment Book.
- `[Observed: dev, Super Admin GB, 2026-09-15]` Không phát hiện route sidebar mới so với baseline `GP - paco assist` ngày 2026-09-14. Kết luận này chỉ áp dụng cho visible global navigation; route ẩn theo deep link, permission-specific contextual menu hoặc feature flag chưa được loại trừ.
- Evidence local, raw: `test-results/product-survey/20260915-092204/sidebar-map.json` (không chứa credential, cookie, token hoặc PII).

### Hidden/contextual route discovery — batch 16

- `[Observed: dev, Super Admin GB, 2026-09-15]` Bounded read-only pass inspected 6 meaningful states: `Patient Search`, its `Filters` drawer, `Unallocated`, Case Workboard entry, Task Workboard entry, and `Configuration` navigation attempt. No previously-unmapped route was verified.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Patient Search > Filters` remains an in-page drawer on `/paco/patient-search`; it does not create a deep link. Mutation-shaped `Save` was not used.
- `[Observed: dev, Super Admin GB, 2026-09-15]` Case Workboard entry `/paco/workboards?tab=case` resolved to a board-specific deep link `/paco/workboards/58`. This URL family was already known; board ID is environment data, not a new product route type.
- `[Observed: dev, Super Admin GB, 2026-09-15]` Attempting to continue at `Configuration` redirected to `/paco/login`; survey stopped as `Blocked: Authentication expired`. No credentials or auth state were captured.
- Evidence local, raw: `test-results/product-survey/20260915-092552/` (review before sharing; controls may include environment labels).

### Hidden/contextual route discovery — batch 17

- `[Observed: dev, Super Admin GB, 2026-09-15]` Sau re-authentication, `Patient Search > row Actions` hiển thị đúng 6 action đã biết: `Profile`, `View`, `Edit`, `View Audit`, `Archive`, `Save for offline access`. Không có action riêng cho role `Super Admin GB`; các action mutation/unknown không được mở.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Actions > Profile` mở `/paco/patient-profile/<patient-guid>/dashboard`. 12 child route đã biết (`dashboard`, `timeline`, `coding`, `consultations`, `medications`, `patient-comms`, `shared-records`, `investigations`, `appointments`, `documents`, `payments`, `tasks`) đều resolve dưới role này; không thấy tab hoặc URL segment mới.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Actions > View Audit` không đổi URL khỏi `/paco/patient-search`; không tạo deep link. Không có dialog ổn định được capture trong lần mở này.
- `[Observed: dev, Super Admin GB, 2026-09-15]` Batch đạt bound 12 meaningful patient-profile views; survey dừng đúng checkpoint thay vì mở rộng sang menu khác.
- Evidence local, raw: `test-results/product-survey/20260915-094748/` (patient identity đã được lược khỏi docs; raw local không chia sẻ).

### Patient actions — batch 18

- `[Observed: dev, Super Admin GB, 2026-09-15]` Patient-profile `Patient actions` menu exposes 16 items: `New Consultation`, `Quick Script`, `Quick Book`, `Quick Send`, `Quick Form`, `Care Navigation`, `Open Patient in EPR`, `Pull Patient Record`, `Quick Pay`, `Create Task`, `PACO Talk`, `Edit Details`, `Update relationships`, `Save for offline access`, `Investigations`, `Show Audit`.
- `[Observed: dev, Super Admin GB, 2026-09-15]` Safe action `Investigations` opens `/paco/patient-profile/<patient-guid>/test-results`, a newly verified hidden URL alias for the visible `Investigations` area previously recorded as `/investigations`. Page landmarks remain `Results`, `Requested`, `+ New Request`, `ICE`; mutation action `+ New Request` was not used.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `/test-results` displayed `Failed to load ICE. Please try again.` in this run. This is current observed behavior, not proof of intended behavior.
- `[Observed: dev, Super Admin GB, 2026-09-15]` Safe action `Show Audit` stays on the current patient-profile URL and opens an in-page dialog with `View: Medications`, `Refresh`, `Date & Time`, `Close`; it creates no deep link. `Refresh` was not used.
- `[Observed: dev, Super Admin GB, 2026-09-15]` Remaining 14 patient actions were not opened because they are mutation-shaped, external-system-shaped, or have unknown persistence. Mutation: `None`.
- Evidence local, raw: `test-results/product-survey/20260915-095540/` (patient identifiers excluded from docs; raw local evidence is not shareable).

### Read-only follow-up — batch 19

- `[Observed: dev, Super Admin GB, 2026-09-15]` Pass chạy tuần tự `Unallocated` → Case Workboard → `Configuration`, dùng auth browser local qua CDP. Tổng cộng 5 meaningful states: ba landing page, `Unallocated > Filters`, và Case Workboard `List View`. Mutation: `None`.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Unallocated` hiển thị `Unallocated (0)`, `List View`, `Create new case`, sort `Breaching (high to low)`, `Filters`, tab `All`, cùng legend `PDS Validated` / `PDS Validation Pending` / `PDS Not Matched`. `Filters` mở drawer với `Clear all`, `Cancel`, `Save`; drawer còn ở trạng thái `Loading filters` khi capture. `Save` không được dùng.
- `[Observed: dev, Super Admin GB, 2026-09-15]` Case Workboard entry resolve tới `/paco/workboards/58`, chọn `Master Case Board`; global counts là `Tasks (0)` và `Cases (0)`. Vì board không có case, không có card menu hoặc `View Audit` để khảo sát trong pass này.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Configuration > Organisation > General` hiển thị child navigation `Practice Profiles`, `Locations`, `Skills`, `Code Rule`, `Sharing Agreements`, `Pathways Config`, `Services`, `Dx Priority`, `Org Priority`, `Inbound Priority Flow`, `Announcements`, `Integrations`; các top-level area khác gồm `Patient`, `Appointment Books`, `Quick Pay`, `Patients & Proxy`, `Users & Staff`, `Clinical Config`, `Case Prioritisation`.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Configuration > General` báo `No Main care site found for this organisation. Please add a care site to manage address and contact info.` Trang chứa mutation controls `Upload Photo`, `Remove Photo`, `Edit Address`, `+ Add Special Opening Hours`, rich-text editors cho `Standard Header`/`Standard Footer`, và `Save`; không control nào được dùng.
- Evidence local, raw: `test-results/product-survey/2026-09-15T03-38-31/` (ảnh và `observations.json`; raw evidence có thể chứa environment identity, không chia sẻ trực tiếp).

### Configuration organisation survey — batch 20

- `[Observed: dev, Super Admin GB, 2026-09-15]` Bounded pass captured 12 `Configuration > Organisation` states. `General`, `Locations`, `Sharing Agreements`, `Services`, `Inbound Priority Flow`, and `Announcements` rendered feature content. Mutation controls were only observed; none were used.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Locations` lists `Assigned Staff`, `Name`, `Address`, `Contact Number`, `Email Address`, `Actions`, plus `Add Location`. `Sharing Agreements` lists organisations with `ODS Code`, `Name`, `Address`, `Access`, `Status`; visible access includes prescribing, consultations, documents, and codings.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Services` exposes repeated `Service ID`, `Service Name`, `Case Type` rows (visible values `111`, `OOH / IUC`, `CAS`, `PCAS`) plus `Add Another` and `Save`.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Inbound priority rules engine` contains `Questions` and `Outcomes`, an entry-node selector, `Preview walk`, `Reset to default`, `Add`, `Delete`, and `Save`. Visible outcomes include `Call 111`, `20-minute DCA`, `Consider 999`, `2-hour home visit`, `Local pharmacy`, `DCA at identified priority`, `Call back once assessed`, and `Unexpected death — follow organisation policy`.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Announcements` contains `Welcome Message`, `Apply to locations`, `Service Updates`, `Add another`, `Delete`, and `Save`.
- `[Inferred: automation limitation, 2026-09-15]` `Practice Profiles`, `Code Rule`, `Pathways Config`, `Dx Priority`, and `Org Priority` were reached through guessed fallback URLs after their visible navigation items were not semantic links; those guesses produced `Page Not Found` and do not establish that the real UI destinations are broken. `Skills` rendered blank at the guessed URL. These six items require retry by clicking their visible navigation controls, not by direct guessed routes.
- Evidence local, raw: `test-results/product-survey/2026-09-15T03-41-16/`.

### Configuration navigation controls — batch 21

- `[Observed: dev, Super Admin GB, 2026-09-15]` Bounded pass clicked 12 visible `Configuration` navigation controls. `Skills`, `Code Rule`, `DX Priorities`, `Org Priorities`, and `Integrations` reached distinct destinations; mutation controls were not used.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Skills` opens `/paco/configuration/organisation/skills` and lists `Name`, `Created By`, `Pathways`, `RBAC Skill`, `Snomed CT Code`, `Actions`; `Add Skill` was not used.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Code Rule` opens legacy-looking `/configuration/#code-rules-config`, showing `Last Updated: N/A` and two `Select an Organisation` controls. No selection was made.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Dx Priority` opens `/paco/configuration/organisation/dx-priorities`; rows map `Disposition Code`/`Disposition Text` to `Service` and `Priority`, with `Add Service & Priority` and `Save` controls left untouched.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Org Priority` opens `/paco/configuration/organisation/org-priorities`, exposing `Priorities`, `Priority Grouping`, `Total Time`, `Breaching Red`, `Breaching Amber`, `Breaching Green`, `Case Ranking`, and `Default`; `Add new`, `Edit`, and `Save` were not used.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Integrations` opens `/paco/configuration/organisation/integrations`; only heading and `Save` were visible in this capture.
- `[Observed: dev, Super Admin GB, 2026-09-15]` Clicking `Practice Profiles` and `Pathways Config` produced `/paco/configuration/organisation/practice-profiles` and `/paco/configuration/organisation/pathways`, both rendering `Page Not Found`. This time routes came from real visible controls, not guessed URLs.
- `[Observed: dev, Super Admin GB, 2026-09-15]` Clicking top-level `Patient`, `Appointment Books`, `Quick Pay`, `Patients & Proxy`, and `Users & Staff` expands child navigation while retaining current `General` page. Visible child items: Patient (`DFD`, `Care Navigation`); Appointment Books (`Scheduler`, `Appointment Books`, `Sessions`, `Slot Types`, `Appointments`, `External Appt Reminders`); Quick Pay (`Product Catalogue`, `Accounts`); Patients & Proxy (`Role Groups`); Users & Staff (`Staff Profiles`, `Role Groups`, `Teams`). These child items remain pending.
- Evidence local, raw: `test-results/product-survey/2026-09-15T03-48-23/`.

### Configuration child screens — batch 22

- `[Observed: dev, Super Admin GB, 2026-09-15]` Bounded pass attempted 12 child navigation states. Verified destinations: `Patient > DFD` opens legacy `/configuration/#dfd-config` but rendered blank; `Appointment Books > Slot Types` opens `/paco-connect/configuration/#service-types` but rendered blank; `External Appt Reminders` opens `/configuration/#externalApptReminders` and remained at `Loading...`; `Quick Pay > Product Catalogue` opens `/paco-connect/quick-pay/products` but rendered blank in this capture.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Patients & Proxy > Role Groups` opens `/paco/configuration/proxy/role-groups`. It lists `Role`, `Role Group`, `RBAC Role`, and `RBAC Activities`; visible permissions span patient access, workboards, appointments, audits, configuration, communications, reports, documents, tasks, prescriptions, and integrations. `Add New` and `Save` were not used.
- `[Inferred: automation limitation, 2026-09-15]` `Patient > Care Navigation`, `Appointment Books > Scheduler`/`Sessions`/`Appointments`, `Quick Pay > Accounts`, and `Users & Staff > Staff Profiles` were not captured because their parent group was not visible after repeated page resets. `Appointment Books > Appointment Books` matched its parent label, so selector ambiguity left the page on `Organisation > General`. These states remain pending and are not treated as product failures.
- Evidence local, raw: `test-results/product-survey/2026-09-15T03-51-52/` (contains role/permission inventory; keep local).

### Configuration child screens — batch 23

- `[Observed: dev, Super Admin GB, 2026-09-15]` Read-only pass verified: `Patient > Care Navigation` → `/configuration/#care-navigation-config`; `Appointment Books > Scheduler` → `/configuration/#scheduler-config`; `Appointment Books` → `/paco-connect/configuration/#appointment-books`; `Sessions` → `/paco-connect/configuration/#clinics`; `Appointments` → `/configuration/#appointments`; `Quick Pay > Accounts` → `/paco-connect/quick-pay/accounts`.
- `[Observed: dev, Super Admin GB, 2026-09-15]` Các destination trên không có visible content sau bounded wait trong pass này. Chưa coi là product failure vì các app này có lịch sử tải chậm và route reset bất đồng bộ.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Users & Staff > Role Groups` mở `/paco/configuration/staff/role-groups`; `Teams` mở `/paco/configuration/staff/teams`, hiển thị `Team Name`, `Assign Staff`, `Add New`, `Save`.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Case Prioritisation` mở `/paco/configuration/case-prioritisation`, hiển thị factors `Patient`, `SPN`, `Age`, `Number cases in last 72hours`, `Day`, `Case Priority`, `Breach Time`, `Out of Hours`; `Add Another` và `Save` không được dùng.
- `Staff Profiles` và `Clinical Config` chưa capture vì parent navigation không xuất hiện ổn định sau route reset. Mutation: `None`.
- Evidence local, raw: `test-results/product-survey/20260915-continue-config/`. Workflow detail: `docs/product/workflows/configuration.md`.

### Configuration follow-up — batch 24

- `[Observed: dev, Super Admin GB, 2026-09-15]` `Users & Staff > Staff Profiles` mở `/paco/configuration/staff/profiles`, với tabs `Active`, `Archived`, `My Profile`; columns `First Name`, `Last Name`, `Gender`, `Email`, `Actions`; `Add Care Professional` không được dùng.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Clinical Config` mở hai child navigation `Risk Strat Builder` và `Template Library`; route vẫn ở `/paco/configuration/organisation/general` cho tới khi chọn child. Các child giả định khác (`Drug Interactions`, `Prescribing`, `Documents`, `Investigations`) không tồn tại trong visible navigation và không được coi là route.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Care Navigation`, `Scheduler` và `Quick Pay > Accounts` vẫn có body blank sau pass thứ hai với bounded wait dài hơn. Trạng thái lặp lại được ghi nhận nhưng chưa đủ trusted expected basis để gọi là defect.
- Mutation: `None`. Evidence local: `test-results/product-survey/20260915-config-followup/`.

### Configuration clinical config — batch 25

- `[Observed: dev, Super Admin GB, 2026-09-15]` `Risk Strat Builder` mở `/paco/configuration/clinical-config/risk-strat-builder`; screen gồm model list, total `Base`/`Max`, `RAG rating thresholds`, và `Risk Factors` dựa trên `Clinical Codes`. Toàn bộ controls tạo, sửa, lưu và xóa không được dùng.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Template Library` là submenu gồm `Document Templates`, `Consultation Templates`, `Prescribing Formularies`. `Document Templates` hiển thị table metadata và actions; `Consultation Templates` hiển thị cards với style/type và created date. Không mở detail và không dùng `Create`, `Edit`, `Rename`, `Delete`.
- `[Observed: dev, Super Admin GB, 2026-09-15]` `Prescribing Formularies` chuyển tới `/paco/configuration/`, chỉ hiện `Please select a menu item`. Chưa đủ trusted expected basis để gọi defect.
- Mutation: `None`. Evidence local, có environment-sensitive data: `test-results/product-survey/20260915-clinical-config/`. Workflow detail: `docs/product/workflows/configuration.md`.

### Batch 28 — Health Forms Designer V2 vs V1 + Inbox + My Case (2026-09-16)

- `[Observed: dev, Super Admin GB, 2026-09-16]` `Health Forms > Designer V2` (`/paco/health-forms`) has its own top nav bar with "Health Forms Designer V2" label; heading `HEALTH FORM TEMPLATES`; tabs `All Templates` | `Shared Templates`; `Filters` button and a `+` (create new) action. Table columns: `Name`, `Type`, `Created date`, `Created by`, `Updated date`, `Actions`. One visible row: `Blinx Health Check In Form` / `Annual Review` / `12 Sept 2026` / `Johnny Bravo` / `12 Sept 2026`; Actions `⋮` menu shows `Edit`, `Preview`, `Duplicate`, `Delete`. All mutation controls not used.
- `[Observed: dev, Super Admin GB, 2026-09-16]` `Health Forms > Designer` (old, `/health-forms/builder/`) is visually and structurally distinct: heading `HEALTH FORM BUILDER`; `New Health Form` button; table columns `Health Form type`, `Health Form Reviewer`, `Health Form Name`, `Created Date`, `Created By`, `Created By Organisation`, `Updated By`, `Shared To Organisation(s)`. Same `Blinx Health Check In Form` row visible (created by Johnny Bravo / Blinx Healthcare / 12 Sept 2026). The two Designers are confirmed as separate implementations with different data models, not versions of the same tool.
- `[Observed: dev, Super Admin GB, 2026-09-16]` `Health Forms > Inbox` (`/health-forms/responses/`) shows the same `All (58,386)` / `Incomplete (47,273)` / `Overdue (11,642)` / `Completed (11,113)` / `Awaiting Review (121)` / `Reviewed (10,992)` stat tiles as batch 8. Below: two tabs `Alarm Triggered` and `No Alarm Triggered`, each with `Health Form` / `Synced` / `Action` columns and `Review Form` buttons; a bell/notification icon pair (top-right) indicating unread/pending; `Search` field and `Filters` control; `All Health Forms` panel below with the same left-hand tree (`All`, `Incomplete`, `Overdue`, `Completed`, `Awaiting Review`, `Reviewed`) and `View By Patient` / `View By Health Form` toggle.
- `[Observed: dev, Super Admin GB, 2026-09-16]` `Case Load Management > My Case` (`/paco/my-case`) shows `MY CASE` heading with `No cases found for you.` — a genuine empty state, not a loading or error state. Controls visible: `Filters` and `List View` toggle; no action controls accessible in this state.
- Mutation: `None`. Evidence local: `test-results/product-survey/batch28/`.

### Batch 27 — Patient Analyser tabs + Manager Dashboard + Virtual Appointments + Appointment Book (2026-09-16)

- `[Observed: dev, Super Admin GB, 2026-09-16]` `/patient-analyser-new/` is a single app with 4 tabs: `PATIENT ANALYSER`, `MEDICATION ANALYSER`, `QOF REGISTERS`, `PATIENT DETAILS` — all accessible via the tab strip at the same URL.
- `[Observed: dev, Super Admin GB, 2026-09-16]` `PATIENT ANALYSER` tab: heading `Patient & Medication Analyser`; stat tiles `Population: 2,000` | `Registry Quality: 19.2%` | `Patients on DMARDs: 0` | `Patients on DMARDs: 29`; left `QOF Register Filter` panel listing 16 registers (Depression Recorded, Asthma, Atrial Fibrillation, BP, ...); right results table with columns `Name`, `NHS Number`, `DOB`, `Gender`, `Age`, `Action`. Searching `Abbott` narrowed results to 2 patients. No data entry or export was performed.
- `[Observed: dev, Super Admin GB, 2026-09-16]` `MEDICATION ANALYSER` tab: heading `Patient & Medication Analyser`; stat tile `Patients on DMARDs: 29`; left filter panel with medication groups and `Acute`/`Repeat` toggles; results table with columns `Drug`, `Dose`, `Units`, `Frequency`, `Review Date`. Search and export controls present but not used.
- `[Observed: dev, Super Admin GB, 2026-09-16]` `QOF REGISTERS` tab: 16 register rows visible, each with a `Quality Indicator` bar (filled/empty segments), `Quality` % value, and `Patients (N)` count. Registers include Depression Recorded, Asthma, Atrial Fibrillation, BP, Cervical Screening, CHF, CKD, COPD, Dementia, Diabetes, Heart Failure, Hypertension, Hypothyroid, Learning Disability, Mental Health, Obesity. `Save Report`, `Reset View`, `Expand All`, `Collapse All` controls present but not used.
- `[Observed: dev, Super Admin GB, 2026-09-16]` `PATIENT DETAILS` tab: heading `Patient & Medication Analyser`; left panel `Gender`, `Age Range` (slider), `Postcode Area`, `Ethnicity`, `Language` filters; results table with columns `Name`, `NHS Number`, `DOB`, `Gender`, `Age`, `Postcode`, `Ethnicity`, `Action`. No patient search by name/number in this tab — only demographic filters.
- `[Observed: dev, Super Admin GB, 2026-09-16]` `Manager Dashboard > Staff Performance` expand/collapse (clicking heading toggles panel visibility) reveals a sortable table with columns `Staff`, `Cases assigned`, `Resolved`, `SLA %`, `Avg Handle Time`; 13 staff listed (initials avatars + name). Sort arrows on all column headers; no column sort applied. Data is real (organisation-specific).
- `[Observed: dev, Super Admin GB, 2026-09-16]` `Manager Dashboard > CQC Readying` shows 5 actions with colored priority badges (green, amber, red, blue) and status indicators; clicking the heading did not expand or navigate — whether it is display-only or has edit actions is unconfirmed.
- `[Observed: dev, Super Admin GB, 2026-09-16]` `Virtual Appointments` (`/web-chat/appointments/`): filters `All Appointment Types`, `Pre-booked Appointments` (Web Chats: 2, Video Consultations: 1), `On Demand Appointments`; `Date Range` control; results table columns `Patient Name`, `Appt. Type`, `Status`, `Actions`. Clicking `On Demand Appointments` filter updates the results panel below.
- `[Observed: dev, Super Admin GB, 2026-09-16]` `Appointment Book` (`/paco-connect/appointment-book`): visible counters `Sessions: 1`, `Booked: 1`, `Available: 0`, `LIVE` badge; `Day`/`Week`/`Month`/`Year` view toggle; left panel with session/bookings; calendar-style book navigation. State is real organisation data.
- `[Observed: dev, Super Admin GB, 2026-09-16]` `Analytics & Reports > Reports` (`/paco/analytics-reports/scr-reports`): consent search field, `Export` button, column controls, and filter controls over SCR records. Filters include `Reporter Role`, `Date Range`, `Patient Search`, `Scr Time`, `SCR Status` (`Pending`/`In Progress`/`Complete`/`Failed`/`Cancelled`). Real data in this run (0 records shown).
- Mutation: `None`. Evidence local: `test-results/product-survey/batch27/`.

### Batch 26 — template detail + Comms Hub + Manager + Analytics (2026-09-16)

- `[Observed: dev, Super Admin GB, 2026-09-16]` `Configuration > Clinical Config > Document Templates` (`/paco/configuration/clinical-config/document-templates`) shows a table with columns `Name`, `Category`, `Merge Fields`, `Author`, `Created`, `Actions`. Row-click opens an inline form (`Edit template name`, `Name` input pre-filled, `CANCEL` and `Save` buttons present; `CANCEL` is a safe close action that does not persist). Confirmed: clicking a row does not auto-save or auto-navigate — `CANCEL` safely closes the form.
- `[Observed: dev, Super Admin GB, 2026-09-16]` `Configuration > Clinical Config > Consultation Templates` (`/paco/configuration/clinical-config/consultation-templates`) shows cards with `T-PALLAS-GP-Partnership-2`, `Style: Blank`, `Created: 11 Sept 2026`; `T-PALLAS-GP-Partnership-1` and `T-PALLAS-GP-Partnership-3` also visible. Card-click opens a side panel with `T-PALLAS-GP-Partnership-2` heading, a template preview area (showing `\n\nDate:  \n\nPatient:  \n\n\n\n`), `CANCEL` and `Preview` buttons. `CANCEL` safely closes the panel.
- `[Observed: dev, Super Admin GB, 2026-09-16]` `Analytics & Reports > Capacity & Demand` (`/capacity-demand/`) shows a bar chart (`Demand vs Capacity`), a date range filter (July 2025 – August 2026, `Apply`), `Organisation`, `Service`, and `Location` dropdowns, and `Demand Summary` / `Capacity Summary` stat tiles showing `Total Demand`, `Total Capacity`, `Uptake %`. Data is real (organisation-specific, not a placeholder).
- `[Observed: dev, Super Admin GB, 2026-09-16]` `Dashboards > Manager` (`/paco/dashboard/manager-dashboards/overview`) shows `Organisation Tasks` (10, `+ New Task`), `Staff Performance` (13 staff with `Cases assigned`, `Resolved`, `SLA %`, `Avg Handle Time`; avatar/initials + name), `Patient` tile, `CQC Readying` (5 actions, green/amber/red/blue badges), `Message Outbox` (0), `Comms Analytics` (0). `Staff Performance` table includes sort on each column header.
- `[Observed: dev, Super Admin GB, 2026-09-16]` Comms Hub `Template Builder` (`https://nhs-comms-hub-dev.blinxhealthcare.com/commshub/template-builder`) shows 10 templates in the table (SMS and Email types, e.g. `GP-Email-Template`, `GP-SMS-Template`, `NHS-Email-Template`, `NHS-SMS-Template`), status `Published`, `Created by: Johnny Bravo`, dates in Sept 2026. `Patient Manager` still hits `error-at-axios-interceptor` redirect (`Welcome Back` / `Sign In`) — this behavior is consistent with batch 4 observation from 2026-09-14, confirming it is a stable bug/gap, not a transient session issue.
- `[Observed: dev, Super Admin GB, 2026-09-16]` Comms Hub `Campaign Manager` (`https://nhs-comms-hub-dev.blinxhealthcare.com/commshub/campaign-manager`) shows 4 campaigns (`Weekly Campaign`, `GP NHS App Campaign`, `Redmoor GP Practice`, `Email Campaign for MAC`) with status `Sent`, channel `Email`, 4,950 / 3 / 1,100 recipients, sent dates in Sept 2026. Filter panel shows `Campaign Statuses` multi-select with all known statuses listed; no mutation controls used.
- Mutation: `None`. Evidence local: `test-results/product-survey/batch26/`.

## Open questions

- Does clicking `QOF Registers` or `Patient Details` in the sidebar actually switch the in-page tab on `/patient-analyser-new/`, or does it always land on the last/default tab? (batch 5 confirmed the app itself renders fine, but tab-switch-on-sidebar-click is still unconfirmed)
- Why does `Comms Hub > Patient Manager` specifically hit `error-at-axios-interceptor` while `Template Manager` and `Campaign Manager` in the same app load correctly? Batch 26 (2026-09-16) confirms this is a stable reproduction — not transient. Likely a session-token or endpoint issue specific to Patient Manager's API call.
- What permissions distinguish role `GP - paco assist` from other roles? Confirmed so far: this role cannot view Audit Trail data anywhere the feature appears — `View Audit` fails identically with "You don't have permission to view Audits logs..." on `Unallocated` cards, `Case Workboards` cards, and now (batch 14) individual clinical consultations in the Patient Profile. Three independent, unrelated features all blocked the same way confirms this is a genuine role-wide permission gap, not a per-feature quirk. Which other actions (e.g. `Change Priority`, `Close Case`, `Move Case`, `Assign Skills`, `Assign Case`, creating consultations/prescriptions/documents/tasks) are actually permitted vs. blocked for this role is unconfirmed since they were not clicked.
- What do dashboard time ranges and carousel pages measure?
- What behavior and data appear inside each mapped child route (deeper than landing-page landmarks)? Patient Profile selected empty/demo states now have reusable detail in [`workflows/patient-profile.md`](workflows/patient-profile.md); populated states remain open.
- Why does `Patients > Care Navigation` route to `/patient-search/` (missing `/paco` prefix) and render a client-side exception instead of a Care Navigation feature page?
- What does the sidebar `Dev Menu Item` entry do?
- Why does the `Appointment Book` sidebar item open a second, differently-scoped `Configuration` surface (`/paco-connect/configuration`) distinct from the main `Configuration` sidebar item's page?
- Why does `Outstanding Reviews`' breadcrumb read `Consultations` instead of matching its sidebar label?
- `Case Workboards` and `Task Workboards` share the `/paco/workboards` URL family (`?tab=case`/`?tab=task`, then a board-specific `/paco/workboards/<id>`), but batch 10 confirmed they are NOT the same feature underneath: Case boards are a single card list with priority sub-tabs and a case-specific menu (`View Audit`, `View PDS`, `Assign Skills`, etc.), while Task boards are a true multi-column kanban with a different task-specific menu (`Change Status`, `Move Task`, `Duplicate`, `Link Task`, etc.) and no audit/PDS actions. Open: why do they share a URL/tab structure despite being architecturally different features?
- Are `Health Forms > Designer` (`/health-forms/builder/`) and `Designer V2` (`/paco/health-forms`) two different form-builder implementations, and which is the current/recommended one? They show different table columns and live on different domains/paths.
- Several heavier apps (`/patient-analyser-new/`, `/health-forms/responses/`, `/health-forms/builder/`, `/paco/health-forms`) take noticeably longer (several seconds) than the main `PACO OS` pages to render content after navigation — is this expected for these specific sub-apps?
- ~~What data or condition triggers the warning icon next to `Address` and `Healthcare Provider` in the `View Patient` dialog's tab strip?~~ Answered in batch 12: it flags a tab with unmet required-field validation (e.g. missing `Address Line 1`/`Postcode`, missing `Usual GP`) for that patient.
- ~~What does `Profile` (vs `View`) show in the Patient Search row `Actions` menu, and how does it differ from the `View Patient` dialog?~~ Answered in batch 13/14: `Profile` navigates to a full clinical patient-record route (`/paco/patient-profile/<id>/...`) with 12 tabs (Dashboard, Timeline, Coding, Consultations & Admin, Medication, Patient Comms, Shared Records, Investigations, Appointments, Documents, Payments, Tasks), entirely distinct from and much larger than the `View Patient` modal. Batch 29 added deeper read-only empty states for `Timeline`, `Coding`, `Documents`, `Investigations`, and `Payments`; populated states remain unexplored.
- Why does the patient-profile `Dashboard` tab's pharmacy widget fail with a raw "Error loading pharmacies" message instead of a graceful empty/error state?
- Does the Patient Profile `Payments` tab genuinely have no content for a patient with no billing activity, or is it gated by a permission/loading issue not yet surfaced? (Rendered as just a bare heading in batch 14.)
- Is `Shared Records`' NHS Smartcard/Care Identity requirement expected to be untestable in this `dev`/demo environment entirely, or is there a test Smartcard/mock flow that should be used instead?
- What does the `ICE` label on the Patient Profile `Investigations` tab refer to? Batch 29 confirmed `Requested` is a distinct safe toggle state; both `Results` and `Requested` showed `No records` for the selected demo patient, so populated behavior remains open.
- Is `Save for offline access` in the Patient Search `Actions` menu a read-only export/cache action, or does it have side effects worth flagging to testers before use?
- Does `View PDS` on an `Unallocated` case card perform a live external NHS PDS (Personal Demographics Service) lookup, or is it a local/cached view? This determines whether it is safe to open during read-only testing.
- What does `white (1)` represent as an `Unallocated` priority tab — a genuine priority tier, a placeholder/default value, or a data quality issue?
- Is the raw-JSON `Patient Name` display in Health Form Inbox's `Awaiting Review` group-by-patient view (for the 87 forms with null `patient_derived_fullname`/`patient_patientguid`) a known/expected edge case for unlinked forms, or an unhandled bug that should get a defect ticket? What do those 87 forms actually contain when expanded?
- What does the blue paper-plane icon next to `Review Form` in the Health Form Inbox `Completed By Patient` tables do (resend to patient? notify? something else)?
- What should happen when a `Time to breach`/`Time Remaining` countdown goes negative (already breached/overdue) — is there meant to be a distinct visual/alert state beyond the countdown continuing into negative numbers? Observed on both the `waiting list` Case Workboard and a Task Workboard's `Journey` section, suggesting this is a shared, systemic gap rather than one-off.
- Is "Epsom PCN Demo - Care Home VIsts" (missing an `i`) a real spelling defect in seed/demo board data, or throwaway test content not worth fixing?
- Why does the Media Library `PATIENT` panel search return no visible results for a known valid patient surname — is there a different required trigger, or is this a genuine defect worth a ticket? (Needs a tester with product knowledge to confirm the intended interaction.)
- Is it intentional that the global header patient-search overlay remains functional-but-invisible on dark-themed sub-app pages like Media Library (where the header bar itself isn't rendered)? Could this confuse a real user who tabs into it via keyboard navigation?
- What do `Assign Case` and `Unassign assignee` (present on Case Workboard cards but not seen on `Unallocated` cards) actually do, and why does `Unallocated` lack them?

## Tester notes
