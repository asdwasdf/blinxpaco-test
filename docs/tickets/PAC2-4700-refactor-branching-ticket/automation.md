# Automation: PAC2-4700

**Input Revision:** 1
**Environment:** dev (base `/paco/` và `pac2-4700-qs-only`)
**Updated:** 2026-09-18

## Feature Location Gate

| Test Case | UI-dependent | Location State | Route Status | Entry/Context/Role/Data Ready | Decision |
|---|---|---|---|---|---|
| PAC2-4700-TC-006 | Yes | valid | Confirmed (base và OS branch) | Quick Send route và `.sort-dropdown` Confirmed; chọn `A - Z` tạo observable alphabetic tree | Yes |
| PAC2-4700-TC-008 | Yes | valid | Confirmed (base và OS branch) | `Patient Reply` chuyển sang `Health Forms`; workaround tester dùng trực tiếp tab `Health Forms` | Yes (regression, không phải business-correct assertion) |
| PAC2-4700-TC-012 | Yes | valid | Confirmed (base và OS branch) | Recheck `blood`: 10 accordion trước/sau, 0 highlight | Yes (regression, không phải business-correct assertion) |
| PAC2-4700-TC-011 | Yes | valid | Confirmed (base và OS branch) | Field `Number` scope từ label parent tới `.p-dropdown`; `+ Add new number` và modal `Enter Number` stable | Yes (UI only) |
| PAC2-4700-TC-014 | Yes | valid | Confirmed (base và OS branch) | Field `Email` scope từ label parent tới `.p-dropdown`; `+ Add new email` và modal `Enter Email` stable | Yes (UI only) |
| PAC2-4700-TC-015 | Yes | valid | Confirmed (base và OS branch) | Quick Send route Confirmed; compose controls Confirmed bằng `title` attribute, không cần chọn campaign cụ thể | Yes |
| PAC2-4700-TC-016 | Yes | valid | Confirmed (base và OS branch) | Quick Send route Confirmed; toggle `View Patient Details` mở panel, accordion `Significant Info` Confirmed | Yes |

`evaluateUiLocationGate()` yêu cầu `route_status: Confirmed` và ordered entry path. Route Dashboard → patient search → `Patient actions menu` → `Quick Send` nay **Confirmed trên base `/paco/`** qua run `20260918-quicksend-base` (xem Execution History). Status tổng ticket vẫn `Candidate` vì các area khác (Booking Links/EMIS) chưa locate.

## Assessment

| Test Case | Decision | Reason | Mutation | Approval |
|---|---|---|---|---|
| PAC2-4700-TC-006 | Yes | **Sửa lại 2026-09-18 (lần 2):** kết luận "Blocked, không có sort control" trước đó (lần 1) là **SAI**. Nguyên nhân sai: control tên hiển thị "By message type" bị nhận diện nhầm là filter tĩnh, nhưng đây chính là **giá trị đang chọn** của dropdown sort thật (`.sort-dropdown` trong wrapper `_custom-treeselect-filter-sort-container_`). Click mở nó ra lộ panel với 5 option: `By date (descending)`, `By date (ascending)`, `By message type`, `A - Z`, `Z - A`. Chọn `A - Z` → tree re-render flat (bỏ category header), 33 `role="treeitem"` theo thứ tự alphabet. Tester manual EXECUTE (status.md, Pass) đã đúng từ đầu | None | Không cần |
| PAC2-4700-TC-008 | Yes (regression) | Expected behavior còn OQ-7 (BA chưa confirm) nhưng actual behavior đã Observed bằng manual test trước đây (status.md: "click nhảy sang tab Health Forms") và re-verify lại đúng trong session này. Automate như regression baseline cho bug đã biết — không assert đây là hành vi đúng, chỉ khẳng định hành vi hiện tại không đổi | None | Không cần |
| PAC2-4700-TC-012 | Yes (regression) | Expected behavior còn OQ-10 (dev chưa confirm) nhưng actual behavior đã Observed (status.md: "gõ 'blood' không filter/highlight gì") và re-verify lại đúng. Automate như regression: `Search all tabs...` input tồn tại, gõ text không đổi số accordion tab hiển thị, không có `mark`/`.highlight` nào | None | Không cần |
| PAC2-4700-TC-015 | Yes | Quick Send route Confirmed; campaign default đã chọn sẵn khi mở dialog (không cần picker); compose controls Confirmed bằng `title`: `Healthcare resources and links` (Resources), `Create a custom button for your email` (Button), `Insert professional email templates` (Templates), `Copy your Email text into the SMS template` (Copy to SMS — khác `Copy to Email` giả định gốc) | None | Không cần |
| PAC2-4700-TC-016 | Yes | Quick Send route Confirmed; toggle thật là button `aria-label="View Patient Details"` (class `patient-details-toggle-btn`) trong dialog header, không phải text "Patient Information". Sau khi click, accordion `Significant Info` và 9 category tab khác Confirmed visible, automate được read-only | None | Không cần |

## Automated Tests

| Test Case | Source | Requirement Basis | Status |
|---|---|---|---|
| PAC2-4700 (route smoke) | `playwright/tests/tickets/PAC2-4700-base.spec.ts` | Quick Send route; Confirmed | Implemented — Pass |
| PAC2-4700-TC-006 | `playwright/tests/tickets/PAC2-4700-base.spec.ts`, `playwright/tests/tickets/PAC2-4700-readonly.spec.ts` | REQ-PAC2-4700-001, REQ-PAC2-4700-007; Confirmed | Implemented — Pass (base và OS branch) |
| PAC2-4700-TC-008 | `playwright/tests/tickets/PAC2-4700-base.spec.ts` | REQ-PAC2-4700-001; Observed (regression, không phải business-correct) | Implemented — Pass |
| PAC2-4700-TC-011 | `playwright/tests/tickets/PAC2-4700-readonly.spec.ts` | Screenshot tester + runtime Observed | Implemented — Pass (OS branch, UI only) |
| PAC2-4700-TC-012 | `playwright/tests/tickets/PAC2-4700-base.spec.ts` | REQ-PAC2-4700-001; Observed (regression, không phải business-correct) | Implemented — Pass |
| PAC2-4700-TC-014 | `playwright/tests/tickets/PAC2-4700-readonly.spec.ts` | Screenshot tester + runtime Observed | Implemented — Pass (OS branch, UI only) |
| PAC2-4700-TC-015 | `playwright/tests/tickets/PAC2-4700-base.spec.ts`, `playwright/tests/tickets/PAC2-4700-readonly.spec.ts` | REQ-PAC2-4700-001, REQ-PAC2-4700-005; Confirmed | Implemented — Pass (base và OS branch) |
| PAC2-4700-TC-016 | `playwright/tests/tickets/PAC2-4700-base.spec.ts`, `playwright/tests/tickets/PAC2-4700-readonly.spec.ts` | REQ-PAC2-4700-009; Confirmed | Implemented — Pass (base và OS branch) |

## Execution History

| Run | Result | Environment | Role | Revision | Evidence |
|---|---|---|---|---|---|
| 2026-09-18-automation-review | Blocked | dev base và `pac2-4700-qs-only` | `Super Admin GB` | 1 | `feature-location.md`; CDP browser running |
| 2026-09-18-quicksend-base | Pass (route only) | dev base `/paco/` | `Super Admin GB` | 1 | `test-results/PAC2-4700/locate/20260918-quicksend-base/patient-result-structure.json` — xác nhận search `Michael Ramella` trả 2 kết quả trùng tên, phân biệt bằng NHS hiển thị `709 86` (không phải chuỗi liền `70986`); `Patient actions menu` mở menu chứa `Quick Send`; click `Quick Send` mở dialog `role="dialog"` (cần wait animation, không tức thời) |
| 2026-09-18-tc006-blocked | Fail | dev base `/paco/` | `Super Admin GB` | 1 | Sau `(click to change)` trong Quick Send dialog, campaign list dùng `role="treeitem"` (tree), không phải `role="option"`; items quan sát được (`Quick Send Template GB1`, `GP Requested Appointment`, `RH QS Test 0510`, `Body Weight Quick Send`, `Test wording on modal`, `Date change`, `Scheduled 3`, `Tower House Practice - New Online Booking!`, ...) không theo thứ tự A-Z; không quan sát được sort control. `role="option"` thấy trước đó là false positive từ font-size dropdown của rich-text toolbar |
| 2026-09-18-tc006-exhaustive-locate | Fail (không tìm thấy) | dev base `/paco/` | `Super Admin GB` | 1 | `test-results/PAC2-4700/locate/20260918-quicksend-base/campaign-sort-candidates.json` — dump toàn bộ control trong dialog (header + picker top bar + tree): không có text/aria-label/title nào chứa "sort"/"A-Z"/"Z-A". Mở Preferences panel (gear icon `sidebar-settings-btn`) — chỉ có `Open by default`, `Show campaigns on open`, `Compact view`/`Hide template names`, filter SMS/Email checkbox. Kết luận: TC-006 không có cơ sở trên base ở phiên bản hiện tại |
| 2026-09-18-tc016-blocked | Fail | dev base `/paco/` | `Super Admin GB` | 1 | `patient-information-accordion.json` — `Patient Information` là `<p>` ẩn trong `patient__contact-details`; accordion tabs tồn tại trong DOM nhưng collapsed, chưa tìm được toggle |
| 2026-09-18-route-smoke | Pass | dev base `/paco/` | `Super Admin GB` | 1 | `PAC2-4700 base: Quick Send dialog opens for patient (route smoke)` — 1 passed, 3 skipped |
| 2026-09-18-tc008-tc012-regression | Pass | dev base `/paco/` | `Super Admin GB` | 1 | `test-results/PAC2-4700/locate/20260918-quicksend-base/tc008-tc012-candidates.json` — re-verify actual behavior: click `[title="Set up Patient Reply"]` → tab `Health Forms` (`getByRole('button', { name: 'tab-Health Forms' })`) trở thành active, khớp Observed cũ trong `status.md`. Gõ `"blood"` vào `Search all tabs...` (sau khi mở `View Patient Details`) → 0 `mark`/`.highlight`, số accordion tab hiển thị không đổi (10), khớp Observed cũ |
| 2026-09-18-full-suite | Pass | dev base `/paco/` | `Super Admin GB` | 1 | 5 passed (route smoke, TC-008, TC-012, TC-015, TC-016), 1 skipped (TC-006) |
| 2026-09-18-full-suite-recheck | Blocked | dev base `/paco/` | `Super Admin GB` | 1 | CDP browser (`127.0.0.1:9222`) không còn chạy giữa các lần thực thi — `Blocked: Authentication expired`, không phải regression sản phẩm |
| 2026-09-18-tc016-resolved | Pass | dev base `/paco/` | `Super Admin GB` | 1 | `test-results/PAC2-4700/locate/20260918-quicksend-base/patient-info-toggle-candidates.json` — toggle thật là `getByRole('button', { name: 'View Patient Details' })` (`class="patient-details-toggle-btn"` trong dialog header, `aria-label="View Patient Details"`). Sau click, accordion `Significant Info` mở; category tab list Confirmed: `Personal Info`, `Significant Info`, `Allergies`, `Active Medications`, `Past Medications`, `Appointments`, `Active Problems`, `Significant Past Problems`, `Test Results`, `Attachments` (không có `PACO Registers` như giả định gốc). Bonus finding: cùng khu vực có button `title="Set up Patient Reply"` — control liên quan TC-008 |
| 2026-09-18-tc006-corrected | Pass | dev base `/paco/` | `Super Admin GB` | 1 | `test-results/PAC2-4700/locate/20260918-quicksend-base/tc006-sort-dropdown-options.json` — sửa lỗi nhận diện sai: "By message type" là giá trị hiện tại của dropdown sort thật (`.sort-dropdown`), không phải filter tĩnh. Mở panel lộ 5 option: `By date (descending)`, `By date (ascending)`, `By message type`, `A - Z`, `Z - A`. Chọn `A - Z` → tree re-render flat, 33 item theo alphabet. Re-run 3/3 stable. Tester manual EXECUTE trước đây (status.md, Pass) đúng; automation lần 1 của tôi sai |
| 2026-09-18-tc007-sampling | Fail | dev base `/paco/` | `Super Admin GB` | 1 | 8/8 lần mở mới Quick Send đều hiện DOB `Unknown (Unknown)`; cộng sample cũ thành 10/11 lần lỗi trên base. Đây không còn là noise 2/3; defect tái lập mạnh. Branch route hiện redirect `/dashboard` nhưng không expose patient search trong wait window, nên không lấy sample branch mới. |
| 2026-09-18-modal-surfaces | Pass (locate) | dev base `/paco/` | `Super Admin GB` | 1 | Quick Send bottom nav confirmed: `Campaign`, `Health Forms`, `Files`, `Booking Link`. `Booking Link` giữ flow trong modal và campaign compose có scheduler-link token/Virtual Consult control; `Files` và `Health Forms` roots confirmed. Không thấy `EMIS`/`slot type` trong state hiện tại. Comms Hub login riêng không cần cho phạm vi consume/render trong modal. |
| 2026-09-18-tc009-recheck | Inconclusive | dev base `/paco/` | `Super Admin GB` | 1 | State hiện tại mở Quick Send với campaign/default content nhưng button `Save` (`title="Save New or Update existing campaign"`) disabled; chọn observed campaign vẫn disabled. Finding cũ “base Save mở dialog” không tái lập, nên chưa thể gọi branch-only regression. Cần xác định điều kiện enable Save trước khi so sánh. |
| 2026-09-18-tc014-save-attempt | Blocked | dev base `/paco/` | `Super Admin GB` | 1 | Probe đầu bị chặn bởi email dropdown có 1,667 options và virtualized overlay; locator ban đầu không scope đúng overlay. Đã sửa locator và chạy lại ở row kế tiếp. |
| 2026-09-18-tc014-save-cleanup | Pass | dev base `/paco/` | `Super Admin GB` | 1 | Tạo email test duy nhất, `Save` thành công, mở lại dropdown xác minh email persist, rồi click trash và xác minh option biến mất. Mutation Persistent đã cleanup hoàn tất; không còn test email. Raw evidence chứa contact data đã xóa sau khi review. |
| 2026-09-18-emis-slot-locate | Inconclusive | dev base `/paco/` | `Super Admin GB` | 1 | Mở `Booking Link` trong Quick Send modal: có button `Booking Link`, nhưng không có text/control `EMIS`, `slot` hoặc `slot type` trong current state. Exact EMIS mapping cần campaign/booking-link context có mapped slot; không tự tạo/chỉnh booking link. |
| 2026-09-18-branch-readonly-parity | Pass (2), Blocked (3), Inconclusive (2) | dev OS branch `/paco/feature-branch/pac2-4700-qs-only/dashboard` | `Super Admin GB` | 1 | Run cũ: TC-015/TC-016 pass; TC-006/011/014 bị false blocker do locator/wait sai; evidence cũ bị Playwright clear ở run sau. |
| 2026-09-18-branch-readonly-parity-corrected | Pass (5) | dev OS branch `/paco/feature-branch/pac2-4700-qs-only/dashboard` | `Super Admin GB` | 1 | Fresh run `5 passed (1.3m)`: TC-006 A-Z; TC-011 `Number`; TC-014 `Email`; TC-015 compose controls; TC-016 Significant Info. UI only, modal add flows đều `Cancel`, mutation None. |
| 2026-09-18-tc009-local-edit | Pass (probe) | dev OS branch `/paco/feature-branch/pac2-4700-qs-only/dashboard` | `Super Admin GB` | 1 | `Save` disabled ở initial/selected-campaign state. Click `Edit`, local SMS text change làm `Save` enabled. Đóng page, không click `Save`; không persist. |
| 2026-09-18-emis-campaign-approved | Inconclusive | dev OS branch `/paco/feature-branch/pac2-4700-qs-only/dashboard` | `Super Admin GB` | 1 | Chọn approved campaign `Tower House Practice - New Online Booking!` thành công; thấy `scheduler_link`/`Booking Link`, không thấy `EMIS`/`slot`/`slot type`. Không `Save`/send/schedule. |
| 2026-09-18-base-standard-suite | Pass (9), Fail (1), Blocked (0) | dev base `/paco/dashboard` | `Super Admin GB` | 1 | Direct Playwright-plugin run STD-01..10: DOB mismatch `Fail`; STD-05 tạo/xóa test number; STD-06 add/remove Health Form local; STD-09 tạo campaign `QA-PAC2-4700-20260919-0001` (`campaignId=56448`, `smsId=11156`), verify trong picker, xóa vĩnh viễn qua Comms Hub `Campaign Manager`, rồi reload/reopen PACO và verify marker absent. Không `Send Now`/`Schedule`; không known leftover. Chi tiết: `exploration.md` section `BASE-OS standard-suite result`. |
| 2026-09-19-branch-os-standard-suite | Pass (9), Fail (1), Blocked (0) | dev OS branch `/paco/feature-branch/pac2-4700-qs-only/dashboard` | `Super Admin GB` | 1 | Direct Playwright-plugin STD-01..10: DOB mismatch parity với base; STD-05 tạo/xóa `07700901947`; STD-06 add/remove `17 jul test by mw`; STD-09 tạo/xóa `QA-PAC2-4700-BRANCH-OS-CAMPAIGN-20260919`, reload/reopen verify absent. Không `Send Now`/`Schedule`; không known leftover. |
| 2026-09-19-branch-connect-standard-suite | Pass (9), Fail (1), Blocked (0) | dev Connect branch `/paco-connect/feature-branch/pac2-4700-qs-only/dashboard`, patient flow sang `/paco/patient-profile/...` | `Super Admin GB` | 1 | Direct Playwright-plugin STD-01..10: DOB mismatch parity; STD-05 tạo/xóa `07700901963`; STD-09 create retry sau `ERR_NETWORK_CHANGED`, tạo/xóa `QA-PAC2-4700-CONNECT-CAMPAIGN-20260919`, reload/reopen verify absent. Không `Send Now`/`Schedule`; không known leftover. |
| 2026-09-19-branch-gp-standard-suite | Pass (10), Fail (0), Blocked (0) | dev GP branch root `/` | `Super Admin GB` | 1 | Direct Playwright-plugin STD-01..10 từ GP Dashboard, không vào `patient-profile/...`: DOB đúng `15/03/2024 (2 years old)`; STD-05 tạo/xóa `07700901984`; STD-06 add/remove `17 jul test by mw`; STD-09 tạo HTTP `200`, verify rồi xóa `QA-PAC2-4700-GP-CAMPAIGN-20260919`; reload/reopen xác minh test number, campaign, draft và booking marker absent. Không `Send Now`/`Schedule`; không known leftover. |
| 2026-09-19-base-tc009-booking-link-follow-up | Pass | dev base `/paco/dashboard` | `Super Admin GB` | 1 | Same-state local SMS edit trên BASE làm `Save` enabled, parity với OS branch; nội dung gốc được khôi phục, không click `Save`. STD-08 được chuẩn hóa theo observable surface: `Date & Time`, `Refresh Availability`, đủ 4 nhóm `Face to Face`/`Phone`/`Video`/`Web Chat Slot Type(s)`, clinician/location, `Booking Notes` và confirmation/reminder render. Bỏ assertion riêng cho EMIS. |
| 2026-09-19-std08-four-target-parity | Pass (4) | BASE-OS, BRANCH-OS, BRANCH-CONNECT, BRANCH-GP | `Super Admin GB` | 1 | Direct plugin run cùng STD-08 trên 4 target: mọi target render date/time, refresh availability, đủ 4 slot-type groups, clinician/location với default `All ... Included`, booking notes và confirmation/reminder. Unique booking-note marker giữ qua Campaign → Booking Link, sau đó clear; không `Save`, `Send Now`, `Schedule`. |
| 2026-09-19-focused-behaviour-parity | Inconclusive (mixed) | OS branch, Connect branch, GP branch | `Super Admin GB` | 1 | Controlled direct-plugin spot measurements: Health Forms reached expected surface khoảng GP `1969ms`, Connect `1942ms`, OS `1990ms` — không tái lập claim GP chậm hơn Connect. Files surface reached expected state trên GP trong wait ≤5s, Connect `6127ms`, OS `130ms`; không target nào permaload trong sample này. Current shared patient exposes email option trên cả GP (`qa.pac2.4700.fixed@example.com`), Connect cùng GP patient state, và OS (`michael@blinxsolutions.com`), nên claim “Connect/GP no patient email” không tái lập. GP/Connect default compose đang SMS (`Copy to Email`), OS sample cũng SMS trong cùng campaign; claim OS defaults Email không tái lập. Sorting/alignment/saved-campaign timing/Test Results inner-scroll chưa đủ controlled evidence trong run này. |
| 2026-09-19-evidence-capture | Pass (evidence) | OS branch, Connect branch, GP branch | `Super Admin GB` | 1 | Screenshot evidence và mô tả claim lưu tại `evidence/README.md` (`IMG-01`..`IMG-08`). Bao phủ compose/default channel, `Files`, `Booking Link`, GP email option và DOB difference. Connect `Files` screenshot ghi transient `Loading...`, được diễn giải cùng runtime `6127ms`, không gắn nhãn permaload. Không `Save`, `Send Now`, `Schedule`; screenshot login vô tình chứa credential autofill đã xóa ngay và không nằm trong evidence set. |

Ghi đè run trước (`2026-09-18-base-execution-attempt`, claim patient không xuất hiện): **sai một phần** — `Michael Ramella` có xuất hiện; nguyên nhân trước đó là locator regex NHS liền `70986` không khớp format hiển thị `709 86`.

## Mutation and Cleanup

**Occurred:** Yes
**Class:** Persistent, temporary test email
**Approval Scope:** User yêu cầu tiếp tục check TC-014, bao gồm flow `Save` đã nêu ngay trước đó
**Cleanup:** Hoàn tất — test email đã xóa và dropdown xác minh không còn option
**Leftover Identifiers:** Không có

## Blockers and Warnings

1. ~~Chạy lại `LOCATE` read-only cho đúng Quick Send flow và cập nhật `feature-location.md` với status `Confirmed`, ordered entry path, landmark/control ở từng bước.~~ **Resolved** — Quick Send route đã confirmed trên base và ghi vào `feature-location.md`.
2. **Resolved:** PACO Connect Dashboard có global `Search...`; search patient rồi chọn result mở PACO OS patient profile, từ đó `Patient actions` → `Quick Send`.
3. TC-008/TC-012 đã automate như **regression baseline** cho actual behavior đã Observed (không phải business-correct assertion). Vẫn cần Resolve OQ-7 và OQ-10 trước khi coi đây là spec chính thức hoặc đóng bug; nếu BA/dev xác nhận đây là bug thật, cần viết case riêng để track fix (test hiện tại sẽ fail khi bug được sửa — đó là mong đợi, cần update test lúc đó).
4. ~~TC-006 không tìm thấy sort control.~~ **Corrected — kết luận trước SAI**. Sort control tồn tại (`.sort-dropdown`, giá trị hiển thị "By message type" khiến bị hiểu nhầm là filter tĩnh). Đã viết lại assertion đúng, automate và pass ổn định (3/3 run). **Bài học quy trình:** khi thấy 1 `<select>`/dropdown hiển thị 1 giá trị cố định, phải luôn thử click mở nó trước khi kết luận "không có control" — không chỉ đọc `textContent` tĩnh.
5. ~~TC-015 cần xác minh lại campaign text cụ thể.~~ **Resolved** — không cần campaign text cụ thể; compose controls Confirmed bằng `title` attribute ngay khi dialog mở, automate và pass.
6. ~~TC-016 cần LOCATE riêng để tìm toggle mở panel `Patient Information`/`Significant Info` trên base.~~ **Resolved** — toggle là `View Patient Details` button; TC-016 automate và pass.
7. `Booking Link`, `Files`, `Health Forms` roots đã locate ngay trong Quick Send modal. Comms Hub origin riêng không cần cho phạm vi consume/render; EMIS slot mapping vẫn thiếu template/context có mapping.
8. TC-007 đã đủ sample base để nâng từ `Inconclusive` lên defect Observed: 8/8 sample mới lỗi, tổng 10/11 lần hiện `Unknown (Unknown)`.
9. TC-009 đã xác định điều kiện enable trên OS branch: `Save` disabled ở initial/campaign-selection state; click `Edit` và thay đổi local SMS draft làm `Save` enabled. Không click `Save`; page đóng nên không persist. Chưa có same-state base run để kết luận parity/regression.
10. ~~TC-014 mutation chưa thực hiện.~~ **Resolved trên base:** `Save` email test thành công, persistence được verify khi mở lại dropdown, rồi email được xóa và xác minh không còn option. Không có leftover.
11. EMIS slot type vẫn `Inconclusive`: campaign `Tower House Practice - New Online Booking!` đã select theo approval; compose có `scheduler_link`/`Booking Link` nhưng không expose `EMIS`, `slot` hoặc `slot type`. Chưa tìm thấy campaign có mapping cần kiểm.
12. OS branch route là `/paco/feature-branch/pac2-4700-qs-only/dashboard`; Connect branch entry là `/paco-connect/feature-branch/pac2-4700-qs-only/dashboard`, sau khi chọn patient chuyển sang PACO OS patient profile theo flow sản phẩm.
13. False blockers TC-006/TC-011/TC-014 đã resolved bằng locator đúng: `.sort-dropdown`, và label parent → `.p-dropdown`. Fresh OS branch suite pass TC-006/011/014/015/016 (`5/5`).
14. TC-008 workaround: dùng trực tiếp tab `Health Forms`; không quan sát alternate `Patient Reply` flow. TC-012 recheck vẫn không filter/highlight (`10 → 10`, highlight `0`). TC-013 được tester chấp nhận: `Esc` đóng dialog là behavior bình thường.
15. Repo-wide `npm run type-check` trước đó fail bởi lỗi có sẵn ngoài phạm vi trong `playwright/tests/survey-super-admin.spec.ts`, `scripts/cd-interactions.ts`, và `scripts/obs-chip-click-test.ts`; output không có lỗi từ `PAC2-4700-readonly.spec.ts`.

## Tester notes

[Protected area]
