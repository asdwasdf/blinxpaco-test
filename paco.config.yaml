product: Paco
environments:
  default: dev
  dev:
    baseUrl: https://blinx.dev.blinxpaco-np.com
    dashboardPath: /paco/dashboard

paths:
  ticketSource: ticket
  ticketOutput: docs/tickets
  testResults: test-results
  playwrightAuth: playwright/.auth

ticket:
  sourcePattern: "^[A-Z][A-Z0-9]*-[0-9]+-[a-z0-9]+(?:-[a-z0-9]+)*$"
  primarySourceFile: ticket.md

safety:
  mutationEnabledEnvironments: [dev]
  allowedHosts: [blinx.dev.blinxpaco-np.com]
  externalDevHosts: []

defaults:
  readOnly: true
  language: vi
  uiTermsLanguage: en
  browser: chromium
  authStrategy: manual
  locateMaxMinutes: 15
  locateMaxViews: 12
  reusableRouteMaxViews: 3
